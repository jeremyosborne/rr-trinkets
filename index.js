//
// This is a stand in file for people using the src-as-module.
// When rollup is run, the modules are packaged individually as well as in
// kitchen sink format.
//
// This file also let's the package.json file work with source code as well
// as with the packaged code without being modified.
//

import * as rrTrinkents from './src'
export default rrTrinkents
